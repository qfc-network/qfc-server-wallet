//! `qfc-server-wallet` binary entrypoint.
//!
//! Status: pre-M1 skeleton; the real entrypoint (HTTP server) lands in M2.
#![forbid(unsafe_code)]
#![warn(missing_docs)]

fn main() {
    println!("qfc-server-wallet pre-M1 skeleton — see docs/server-wallet-rfc.md");
}
